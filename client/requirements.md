## Packages
date-fns | Date formatting and manipulation
framer-motion | Smooth animations for bars and page transitions
lucide-react | Icons (already in base but explicitly required)

## Notes
- Amounts are stored in cents, need dividing by 100 for display
- Currency format is BRL (pt-BR)
- Date format is YYYY-MM-DD
